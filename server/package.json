{
  "name": "server",
  "version": "1.0.0",
  "description": "'News Api Serevr'",
  "main": "index.js",
  "scripts": {
    "start": "nodemon",
    "postpublish": "cd ../client && npm run build && cd ../server && npm install && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "mongoose": "^6.8.3",
    "newsapi": "^2.4.1",
    "nodemon": "^2.0.20"
  }
}
